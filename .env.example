# Copie para ".env" na raiz do projeto (opcional).
# O Docker Compose usa estes valores para o PHP (serviço web) e pode alinhar a BD.
# Valores por omissão coincidem com docker-compose e com www/config/db.php.

DB_HOST=db
DB_NAME=farmacia_jombaca
DB_USER=jombaca_user
DB_PASSWORD=jombaca_mainUser2026

MYSQL_ROOT_PASSWORD=jombacaRoot
